#!/bin/bash # Display menu options echo "Please select an option:" echo "1. List files and directories" echo "2. Option 2 (Placeholder)" echo "3. Option 3 (Placeholder)" read -p "Enter your choice (1/2/3): " choice # Process user choice case $choice in 1) echo "Listing files and directories:" ls ;; 2) echo "Option 2 selected. Functionality to be added." ;; 3) echo "Option 3 selected. Functionality to be added." ;; *) echo "Invalid selection. Exiting." exit 1 ;; esac
